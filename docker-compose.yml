services:
  random-cat:
    build: .
    volumes:
      - node_modules:/usr/src/random-cat/node_modules
      - .:/usr/src/random-cat
    ports:
      - "3000:3000"
    command: tail -f /dev/null
    # command: npm run dev -- --host
volumes:
  node_modules:
